import Window from './Window';

const YearlyStats = async () => {
  return (
    <Window English='Study Time by Year' Japanese='年の勉強' width={'w-[500px]'} height={'h-[300px]'}>
      <div>someday a graph</div>
    </Window>
  );
};

export default YearlyStats;
